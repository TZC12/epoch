# ============================================================
# Epoch · Cloudflare Pages 环境变量说明
#
# 部署方式：项目根目录只放 index.html + supabase/ + _headers +
#           scripts/inject.mjs；构建时 Cloudflare Pages 注入
#           以下两个变量到 index.html。
#
# 本地开发：
#   1. 把本文件复制为 .env（去掉 #），填入真实值
#   2. 运行：npm run dev
#   3. 访问 http://127.0.0.1:8788
#
# Cloudflare Pages 配置（控制台 → Pages 项目 → Settings）：
#   Build command:    node scripts/inject.mjs
#   Build output:     .
#   Environment variables:
#     - SUPABASE_URL
#     - SUPABASE_ANON_KEY
#
# 安全：
#   - anon key 可以放在浏览器端（数据安全由 RLS 保证）
#   - 永远不要把 service_role key 放进前端 / .env / 仓库
#   - .env 已在 .gitignore 中
# ============================================================

SUPABASE_URL=https://你的项目ref.supabase.co
SUPABASE_ANON_KEY=sb_publishable_你的key